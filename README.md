# counting-app

个人记账 PWA（Vite + Svelte + TypeScript）。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

产物输出到 `dist/`。

## 其他命令

```bash
npm run preview   # 预览构建产物
npm run test      # 运行单元测试（vitest）
npm run check     # svelte-check 类型检查
```
